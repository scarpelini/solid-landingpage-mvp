import React from 'react';
import Heading from './Heading';

export const basic = () => (
  <Heading label={'Your MVP ready for the world in a few weeks.'} />
);

export default {
  title: 'Heading',
};
